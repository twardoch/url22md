I’ve changed the CLI: 

```python
def cli(
    url: str | None = None,
    Urls_path: str | None = None,
    format: str = "md",
    output_dir: str | Path = ".",
    jsonl: str | Path | None = None,
    tool: int | None = None,
    proxy: bool = False,
    Force: bool = False,
    minify: bool = False,
    clean: bool = False,
    Clean_all: bool = False,
    Jobs: int = 5,
    Timeout: int = 30,
    verbose: bool = False,
```

'format' should be 

- 'md' for markdown per file 
- 'all' for all files in a single markdown file, separated by Markdown HR + H1 URLs 
- 'json' we put the markdown content into a 'markdown' key in the JSONL report 
- '-' like json but we emit the report into stdout and not into a file

Implement the above. 

Then improve the Python API. By default the Python API needs to have a method that returns an object like the unified JSONL report. There may be a "format=" parameter and then it should also write. But without it, there should be no writing, just returning the object. 

Then update the README.md with the new flags, their descriptions, and the new API. 

